<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.ufpa.smartufpa">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <supports-screens
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="smartufpa.ACTION_MAIN" />
                <action android:name="android.intent.action.SEARCH" />

                <category android:name="smartufpa.CATEGORY_MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity
            android:name=".activities.NoGpsActivity"
            android:theme="@style/AppCompat.FullScreen">
            <intent-filter>
                <action android:name="smartufpa.ACTION_NO_GPS" />

                <category android:name="smartufpa.CATEGORY_NO_GPS" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.RegisterActivity"
            android:theme="@style/AppCompat.FullScreen" />
        <activity
            android:name=".activities.about.AboutActivity"
            android:label="Sobre">
            <intent-filter>
                <action android:name="smartufpa.ACTION_ABOUT" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="smartufpa.CATEGORY_ABOUT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.about.AboutProjectActivity"
            android:label="@string/title_activity_about_project" />
        <activity
            android:name=".activities.about.AboutCodeActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/AppCompat.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.SelectCategoryActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".activities.PermissionCheckActivity"
            android:theme="@style/AppCompat.FullScreen" />
        <activity
            android:name=".activities.NewPlaceActivity"
            android:label="@string/title_activity_new_place_form"
            android:theme="@style/AppTheme" />
        <activity android:name=".activities.EnterScreenActivity">
            <intent-filter>
                <action android:name="smartufpa.ACTION_LOGIN" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="smartufpa.CATEGORY_LOGIN" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.EditElementActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".activities.OsmLoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />

                <data android:scheme="br.ufpa.smartufpa" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.RedirectActivity" />
    </application>

</manifest>